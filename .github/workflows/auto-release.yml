name: Stable Release

on:
  push:
    branches: [ main ]

permissions:
  contents: write

jobs:
  auto-release:
    name: Auto Tag and Release
    uses: mmuhlariholdings/hlavi-workflows/.github/workflows/rust-auto-release.yml@main
    with:
      package-name: 'hlavi-agent'
      create-draft-release: false
      default-bump: 'patch'
    secrets:
      github-token: ${{ secrets.GITHUB_TOKEN }}
